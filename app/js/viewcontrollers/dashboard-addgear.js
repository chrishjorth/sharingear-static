/**
 * Controller for the Sharingear Add gear dashboard page view.
 * @author: Chris Hjorth
 */

define(
	['utilities', 'viewcontroller'],
	function(Utilities, ViewController) {
		var AddGear = Utilities.inherit(ViewController, {
		}); 
		return AddGear;
	}
);