/**
 * Controller for the Sharingear Gear form view.
 * @author: Chris Hjorth
 */

define(
	['viewcontroller'],
	function(ViewController) {
		//var Home = ViewController.inherit();
		var GearForm = ViewController; 
		return GearForm;
	}
);