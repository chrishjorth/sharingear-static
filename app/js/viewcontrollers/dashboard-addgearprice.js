/**
 * Controller for the Sharingear Add gear pricing dashboard page view.
 * @author: Chris Hjorth
 */

define(
	['utilities', 'viewcontroller'],
	function(Utilities, ViewController) {
		var AddGearPrice = Utilities.inherit(ViewController, {
		}); 
		return AddGearPrice;
	}
);